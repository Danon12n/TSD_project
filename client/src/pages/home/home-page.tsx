import { FC} from "react";
import styles from "./home-page.module.css";

const HomePage: FC = () => {

    return (
        <div className={styles.homePageWrapper}>
        </div>
    );
};
export { HomePage };
